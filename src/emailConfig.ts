// ── EmailJS configuration ──────────────────────────────────────────────────────
// Sign up free at https://emailjs.com, connect your Gmail/Outlook service,
// create two email templates (owner notification + customer confirmation),
// then paste the IDs below.
//
// Template variable names to use in your EmailJS templates:
//   {{order_ref}}      — e.g. #RR-1717000000000
//   {{customer_name}}  — full name entered by customer
//   {{phone}}          — phone number
//   {{email}}          — customer email
//   {{address}}        — delivery address
//   {{order_lines}}    — formatted product list (multi-line text)
//   {{total}}          — e.g. KES 1,700
//   {{to_email}}       — recipient (set dynamically per send)

export const EMAILJS_SERVICE_ID    = 'your_service_id'
export const EMAILJS_OWNER_TPL     = 'your_owner_template_id'
export const EMAILJS_CUSTOMER_TPL  = 'your_customer_template_id'
export const EMAILJS_PUBLIC_KEY    = 'your_public_key'
export const OWNER_EMAIL           = 'owner@ratishreigns.com'
